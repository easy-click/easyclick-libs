/**
 * 原作者QQ: 1438667453 楠桃
 * 本软件使用EasyClick iOS免越狱 USB版本开发，适配 v4.1中控台版本
 * 文档地址：https://ieasyclick.com/iosdocs/
 * EasyClick iOS免越狱 USB专业用于开发iOS自动化脚本、iOS免越狱投屏等的软件平台
 * EasyClick iOS免越狱还支持无需任何硬件直接打包ipa的自动化脚本方案
 * 官方地址：https://ieasyclick.com
 * QQ交流群： 
 * Q群1: 777164022 Q群2: 922739785 Q群3：647082990
 * Q群4: 772810035 Q群5: 484379843 Q群6：435253761
 */


function Wj_function() {
}
let WJ = new Wj_function();
var 文件 = false

    Wj_function.prototype.未激活符石1 = function () {
        while (true) {
            let 检测文件是否存在 = file.exists("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt");
            if (检测文件是否存在) {
                //读取一行
                var 未激活符石1 = file.readLine("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt",1);
                sleep(100)
                logd(未激活符石1)
                let 未激活符石 = GJ.数字识别(未激活符石1)
                if (未激活符石) {
                    文件 = true;
                    logd("购买")
                } else {
                    文件 = false
                    logd("价格大于设置价格不购买")
                }
                return 文件;
            } else {
                file.mkdirs("C:/mhck");
                file.create("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt")
                //创建文件夹
            }
        }
    }
    Wj_function.prototype.黑宝石1 = function () {
        while (true) {
            let 检测文件是否存在 = file.exists("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt");
            if (检测文件是否存在) {
                //读取一行
                var 黑宝石1 = file.readLine("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt", 2);
                sleep(100)
                logd(黑宝石1)
                let 黑宝石 = GJ.数字识别(黑宝石1)
                if (黑宝石) {
                    文件 = true;
                    logd("购买")
                } else {
                    文件 = false
                    logd("价格大于设置价格不购买")
                }
                return 文件;
            } else {
                file.mkdirs("C:/mhck");
                file.create("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt")
                //创建文件夹
            }
        }
    }
    Wj_function.prototype.清灵净瓶1 = function () {
        while (true) {
            let 检测文件是否存在 = file.exists("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt");
            if (检测文件是否存在) {
                //读取一行
                var 清灵净瓶1 = file.readLine("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt", 3);
                sleep(100)
                logd(清灵净瓶1)
                let 清灵净瓶 = GJ.数字识别(清灵净瓶1)
                if (清灵净瓶) {
                    文件 = true;
                    logd("购买")
                } else {
                    文件 = false
                    logd("价格大于设置价格不购买")
                }
                return 文件;
            } else {
                file.mkdirs("C:/mhck");
                file.create("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt")
                //创建文件夹
            }
        }
    }
    Wj_function.prototype.速度红色1 = function () {
        while (true) {
            let 检测文件是否存在 = file.exists("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt");
            if (检测文件是否存在) {
                //读取一行
                var 速度红色1 = file.readLine("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt", 4);
                sleep(100)
                logd(速度红色1)
                let 速度红色 = GJ.数字识别(速度红色1)
                if (速度红色) {
                    文件 = true;
                    logd("购买")
                } else {
                    文件 = false
                    logd("价格大于设置价格不购买")
                }
                return 文件;
            } else {
                file.mkdirs("C:/mhck");
                file.create("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt")
                //创建文件夹
            }
        }
    }
    Wj_function.prototype.黄色伤害1 = function () {
        while (true) {
            let 检测文件是否存在 = file.exists("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt");
            if (检测文件是否存在) {
                //读取一行
                var 黄色伤害1 = file.readLine("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt", 5);
                sleep(100)
                logd(黄色伤害1)
                let 黄色伤害 = GJ.数字识别(黄色伤害1)
                if (黄色伤害) {
                    文件 = true;
                    logd("购买")
                } else {
                    文件 = false
                    logd("价格大于设置价格不购买")
                }
                return 文件;
            } else {
                file.mkdirs("C:/mhck");
                file.create("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt")
                //创建文件夹
            }
        }
    }
    Wj_function.prototype.灵篆1 = function () {
    while (true) {
        let 检测文件是否存在 = file.exists("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt");
        if (检测文件是否存在) {
            //读取一行
            var 灵篆1 = file.readLine("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt", 6);
            sleep(100)
            logd(灵篆1)
            let 灵篆 = GJ.数字识别(灵篆1)
            if (灵篆) {
                文件 = true;
                logd("购买")
            } else {
                文件 = false
                logd("价格大于设置价格不购买")
            }
            return 文件;
        } else {
            file.mkdirs("C:/mhck");
            file.create("C:/mhck/设备"+JSON.parse(JSON.stringify(获取设备))+".txt")
            //创建文件夹
        }
    }

}



